import * as React from 'react';
import AnimalList from '../components/AnimalList';

const Home = () => {
    return(
        <div>
            <AnimalList />
        </div>
    );
}

export default Home;